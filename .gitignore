# ------------------------------------------------------------------------------
# The `.gitignore` file tells the Git version control system what content on the
# local filesystem should not be tracked.
#
# Files that may exist within a code repository but that should - generally - be
# excluded from source control include machine-generated files and any artifacts
# that can be derived from the source code.
#
# GitHub provides a collection of useful `.gitignore` templates for a variety of
# software stacks here: https://github.com/github/gitignore
# ------------------------------------------------------------------------------

# System-generated files.

.DS_Store
*.seed
*.swp
Thumbs.db

# Backups and temporary files.

*.bak
*.tmp

# Files generated by other version control systems. The `.git` folder is already
# excluded.

.cvs
.svn

# Personal IDE configurations.

.idea
nbproject
.project
.settings
*.sublime-workspace
*.sublime-project
.vs
.vscode

# Logs.

*.log
*.log*
logs

# Caches generated by various developemnt tools: package managers, task runners,
# test frameworks, etc.

.cache
coverage
.eslintcache
.grunt
lib-cov
.node_repl_history
.npm
.nyc_output
*.pid
*.pid.lock
.vagrant
.yarn-*

# Vendor components that are installed via package managers.

bower_components
dependencies
jspm_packages
node_modules
packages
vendor
vendors

# Environment-specific configurations.

*.env

# Build output and distributable bundles.

build
dist
.next
public
*.tgz

# A place to try out code within the project context.
# And a place for your personal tasks.

.scratch
.todo
