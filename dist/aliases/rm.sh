#!/bin/sh

# ==============================================================================
# Aliases for `rm`.
# ==============================================================================

# Always ask for confirmation before removing files (interactive mode).
alias rm="rm -i"
