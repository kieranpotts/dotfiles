#!/bin/sh

# ==============================================================================
# Git alias for listing all tags, sorted by commit date.
#
# Use `git download` to download all tags, as well as branches, from the
# upstream repository.
#
# Usage:
#
# ```
# git tag
# ```
# ==============================================================================

git tag --list --sort=committerdate
