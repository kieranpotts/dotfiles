= The `bin` directory

The `bin` directory is intended to be added to the user's system PATH, thus making the Git aliases and shell script "binaries" executable from any working directory.

All shell scripts in this directory are POSIX-compliant and will run in all Unix shells. For Windows, a Linux emulator like Git Bash or a virtual machine like WSL2 is required.
