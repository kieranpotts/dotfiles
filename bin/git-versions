#!/bin/sh

# ==============================================================================
# Git alias to list tags prefixed with the lower case letter "v", sorted
# numerically.
#
# Usage:
#
# ```
# git versions
# ```
# ==============================================================================

git tag --list --sort=-version:refname v*
