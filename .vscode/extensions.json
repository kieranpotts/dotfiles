{
  "recommendations": [

    /* NOTE: Keep this list synchronized with `../.devcontainer/devcontainer.json`. */

    /* EditorConfig — .editorconfig file support. */
    "editorconfig.editorconfig",

    /* ShellCheck — Linter for shell scripts. */
    "timonwong.shellcheck",

  ]
}
